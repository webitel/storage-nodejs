{
  "elastic": {
    "enabled": true,
    "httpAuth": "",
    "host": "elasticsearch2:9200",
    "log": "warning",
    "keepAlive": false,
    "requestTimeout": 60000,
    "indexNameCdr": "cdr",
    "indexNameCdrTemplate": "${INDEX}-${LEG}-${YEAR}-${MONTH}-${WEEK}-${DOMAIN}"
  }
}
